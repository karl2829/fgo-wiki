# 从者 MD 生成规则 — 完整版（generate.py 对应）

## 1. 数据源选择
- 从者基础数据：`raw/servants/{id}.json`
- 使用 `noblePhantasms[-1]`（最后一个，即强化后版本，priority 最大）
- 技能使用 `get_current_skills()` 获取当前最高 priority 版本

## 2. 宝具渲染规则

### 2.1 数据结构识别
每个 NP 的 `functions` 数组中：
- `damageNp` / `damageNpIndividual` = 伤害段（主宝具）
- `addStateShort` / `addState` = 先发/后发 Buff
- `gainNp` / `gainStar` = NP/星获取
- `subState` / `lossHp` / `hastenNpturn` = 其他

### 2.2 数值提取规则
| 字段 | NP等级值 (Lv.1-5) | OC等级值 (OC1-5) |
|------|------------------|------------------|
| damageNp | `svals[0-4].Value` | `svals[0].Correction` + `svals2-5[0].Correction` |
| damageNpIndividual | 同上 | 同上 |
| gainNp | `svals[0-4].Value` / 100 | 其他函数的 `svals2-5[0].Value` / 100 |
| gainStar | `svals[0-4].Value` (原始) | 同上 |
| addStateShort | `svals[0-4].Value` / 10 | 同上 |

**OC 回退策略**：
1. 优先从主伤害函数的 `Correction` 取
2. 若无/全等，回退到同 NP 下其他函数的 `svals2-5[0].Value`
3. 仅当 5 个值不全相等时生成 OC 表

### 2.3 描述文本解析
- `detail` 字段按 `▲` / `＋` / `＆` / `▲` 分割
- **伤害描述**：含"攻击"、"特攻"、"发动"的段落
- **OC 描述**：含"过量充能"、"Over Charge"、或以 `<...>` 包裹的段落
- **先发 Buff 描述**：含"宝具"+"威力/上升/提升"的段落

### 2.4 渲染顺序（关键）
```
1. 先发 Buff（伤害前生效，如"自身宝具威力提升"）
2. 伤害描述 + NP 升级表 (Lv.1-5, 5列)
3. OC 描述 + OC 表 (OC1-5, 5列) ——仅有变化时
4. 后发效果（伤害后生效，如 NP 增加、获得星）
```

### 2.5 表格格式
**NP 升级效果（5列）**：
```
### 宝具升级效果（5个等级）

| Lv.1 | Lv.2 | Lv.3 | Lv.4 | Lv.5 |
|---|---|---|---|---|
| 400% | 500% | 550% | 575% | 600% |
```

**OC 效果（5列）**：
```
### Over Charge 效果（5个等级）

| OC1 | OC2 | OC3 | OC4 | OC5 |
|---|---|---|---|---|
| 20% | 27.5% | 35% | 42.5% | 50% |
```

### 2.6 格式化函数 `format_val(v, func_type)`
```
damageNp/damageNpIndividual: 除 10 → "400%"
gainNp:                      除 100 → "27.5%"（保留 1 位小数）
gainStar:                    原始值 → "5"
addStateShort/addState:      除 10 → "30%"
其他/未知:                    除 10（默认）
```

## 3. 技能渲染规则

### 3.1 主动技能（10 列）
- 表头：`Lv.1` ~ `Lv.10`
- 冷却：`充能时间：7→6→5`（只显示变化值）
- 多效果拆分：每效果独立表格
- 固定值：文字描述，不生成表格

### 3.2 职阶技能
- 无等级变化，文字描述
- 格式：`- **[[技能名 等级]]** (ref): 效果`

### 3.3 追加技能
- 同主动技能，10 列表格
- 无冷却显示
- 共享引用文件在 `references/skills/append/`

## 4. 材料需求表（新版）

### 4.1 列结构
| 阶段 | QP | ID | 图标 | 道具名称 | 数量 |

### 4.2 图标 URL
**直接按 ID 构造**：`https://static.atlasacademy.io/CN/Items/{item_id}.png`
（忽略 items DB 的 icon 字段，该字段 627/899 条错误）

### 4.3 总计表
单独成表，每类材料分别统计：
| ID | 图标 | 道具名称 | 总数 |
最后一行：`| — | — | **QP** | **{total_qp:,}** |`

## 5. 字段映射表（从者基础信息）

| 显示字段 | JSON 路径 | 备注 |
|----------|-----------|------|
| ID | `id` | |
| 图鉴编号 | `collectionNo` | |
| 名称 | `name` | |
| 原名 | `originalName` / `_jp_name` | |
| 职阶 | `className` | |
| 星级 | `rarity` | ★×N |
| COST | `cost` | |
| 属性 | `attribute` (earth/human/sky/star/beast) | 中文映射 |
| 性别 | `gender` | |
| 阵营 | `alignment` (policy·personality) | 秩序·善 等 |
| 获取方式 | `type` + `flag` | 常驻/限定/剧情限定/活动赠送... |
| 画师 | `illustrator` / `profile.illustrator` | |
| 声优 | `cv` / `profile.cv` | |
| 特性标签 | `traits[]` | 过滤黑名单，映射中文名 |

## 6. 引用文件路径
- 职阶技能：`references/skills/class/{名}.md`
- 追加技能：`references/skills/append/{名}.md`
- 从者资料：`references/lore/{名}.md`

## 7. 关键函数签名（generate.py）
```python
format_val(v, func_type) -> str
format_np_effects(np_data) -> str  # 返回完整宝具段落
format_skill_detail(sk, icon) -> str
mat_table(title, mats_dict, format_key, offset, show_total) -> str
get_current_skills(skills) -> list
get_scale_div(func_type) -> int  # 0/10/100
```

## 8. 特殊情况处理
- **多段伤害 NP**：每段独立 function，按顺序渲染
- **强化前后 NP 共存**：只渲染最后一个（强化版）
- **OC 值全等**：不生成 OC 表
- **技能强化前后**：`get_current_skills` 自动取最高 priority
- **缺失字段**：用默认值或跳过，不报错
- **宝具 detail 为空**：用兜底描述"对敌方全体发动强大的攻击<宝具升级效果提升>"